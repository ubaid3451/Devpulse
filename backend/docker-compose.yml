services:
  db:
    image: postgres:16-alpine
    container_name: devpulse_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: <PLACEHOLDER>
      POSTGRES_PASSWORD: <PLACEHOLDER>
      POSTGRES_DB: <PLACEHOLDER>
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U <PLACEHOLDER> -d <PLACEHOLDER>"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    container_name: devpulse_api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://<PLACEHOLDER>:<PLACEHOLDER>@db:5432/<PLACEHOLDER>
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./alembic:/app/alembic
      - ./alembic.ini:/app/alembic.ini
    depends_on:
      db:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  postgres_data:
